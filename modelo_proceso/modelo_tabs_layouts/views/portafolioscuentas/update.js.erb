$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#portafolioscuenta_<%= @portafolioscuenta.id %>").replaceWith("<%= j render 'tr', portafolioscuenta: @portafolioscuenta %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
