$("#portafolioscuenta_form").collapse("hide");

$("#portafolioscuenta_form").empty();

<% if @active_record.present? %>
	$("#portafolioscuenta_<%= @active_record.id %>").replaceWith("<%= j render 'tr', portafolioscuenta: @active_record %>");
<% end %>

$("#portafolioscuenta_<%= @portafolioscuenta.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#portafolioscuenta_<%= @portafolioscuenta.id %>").addClass("active_portafolioscuenta");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2027',
    changeMonth: true,
    changeYear: true,
});