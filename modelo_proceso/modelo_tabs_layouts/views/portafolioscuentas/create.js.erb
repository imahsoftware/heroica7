$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @portafolio.portafolioscuentas.count == 1 %>
	$("#portafolioscuentas").html("<%= j render 'tabla' %>");
<% else %>
	$("#tbody_portafolioscuentas").prepend("<%= j render 'tr', portafolioscuenta: @portafolioscuenta %>");
<% end %>

$(".clean").val("").trigger('change');

$("#portafolioscuenta_form").collapse("hide");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

$(".form-group").removeClass("has-error");
$(".form-group > .help-block.help-block-error").remove();

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
