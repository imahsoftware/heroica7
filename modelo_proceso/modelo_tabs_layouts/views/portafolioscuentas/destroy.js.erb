$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

<% if @portafolio.portafolioscuentas.count == 0 %>
	$("#portafolioscuentas").html("<div class='alert alert-danger'><h4><i class='icon fa fa-info'></i> <%=t :atencion%></h4><%=t :atencion_msj%></div>");
<% else %>
	$("#portafolioscuenta_<%= @portafolioscuenta.id %>").remove();
<% end %>

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);

$('.tooltip').tooltip('hide');
