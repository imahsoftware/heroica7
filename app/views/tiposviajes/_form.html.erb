<%= simple_form_for @tiposviaje, remote: true do |f| %>
  <div class="bs-callout">

    <p class="section-label">Datos del tour</p>
    <div class="row">
      <div class="col-md-7">
        <%= f.input :descripcion, label: 'Descripción del tour',
            input_html: { onChange: 'this.value=this.value.toUpperCase()', class: 'form-control input_uppercase' } %>
      </div>
      <div class="col-md-3">
        <%= f.input :valor, label: 'Valor',
            input_html: { class: 'form-control' } %>
      </div>
    </div>

    <div class="row" style="margin-top: 12px;">
      <div class="col-md-6">
        <% if @tiposviaje.new_record? %>
          <%= button_tag t(:cancelar), type: 'button',
              onclick: "$('#tiposviaje_form').html('').removeClass('in').css({height:'0px',overflow:'hidden'}); return false;",
              class: 'btn btn-flat btn-default' %>
        <% else %>
          <%= link_to t(:cancelar), tiposviaje_path(@tiposviaje),
              class: 'btn btn-flat btn-default', remote: true %>
        <% end %>
      </div>
      <div class="col-md-6 text-right">
        <%= f.button :submit, t(:guardar), class: 'btn-guardar' %>
      </div>
    </div>

  </div>
<% end %>
