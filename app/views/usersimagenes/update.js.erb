$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersimagen_<%= @usersimagen.id %>").replaceWith("<%= j render 'tr', usersimagen: @usersimagen %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
